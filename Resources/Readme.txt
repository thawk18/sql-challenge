Data Cleaning Excel:

1. Adjusting matching columns e.g. emp_title_id
2. Conversion of dates from mm/dd/yyyy to dd/mm/yyyy
  I did this in 2 steps:
  a) filtered cells that were already dates and changed format
  b) columns remaining cells that were text, forced into values and changed date format
